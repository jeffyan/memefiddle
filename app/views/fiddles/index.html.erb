<h1>Listing fiddles</h1>

<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @fiddles.each do |fiddle| %>
      <tr>
        <td><%= link_to 'Show', fiddle %></td>
        <td><%= link_to 'Edit', edit_fiddle_path(fiddle) %></td>
        <td><%= link_to 'Destroy', fiddle, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Fiddle', new_fiddle_path %>
